import styled from 'styled-components';

export default function Header() {
  return <Headline>Lean Coffee Board</Headline>;
}

const Headline = styled.h1`
  color: darkslategray;
  text-align: center;
`;
